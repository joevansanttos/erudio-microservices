server:
  port: 8888
spring:
  application:
    name: erudio-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/joevansanttos/erudio-config-server.git
          default-label: main
          search-paths:
          - 'greeting-service*'
         
